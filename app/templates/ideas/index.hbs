<section class="ideas">
  <div class="wrapper">
    <h1>
      Ideas

      {{link-to 'new' 'ideas.new' class='btn btn-primary pull-right'}}
    </h1>

    {{#each controller as |idea|}}
      <div class="idea jumbotron clearfix">
        <div class="author">
          <img src="{{idea.user.image}}"> {{idea.user.name}}
        </div>

        <div class="idea-contents">
          <h2>{{idea.title}}</h2>
          <p>{{idea.description}}</p>
        </div>
      </div>
    {{/each}}
  </div>
</section>

{{outlet}}
